// RUN: %clang_cc1 -x cxc -fsyntax-only %s

forceinline int add(int a, int b) {
    return a + b;
}

forceinline void print_result(int x) {
    printf("Result: %d\n", x);
}

int main() {
    int result = add(5, 3);
    print_result(result);
    return 0;
}
